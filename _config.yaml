# site properties and page defaults
title: HAI lab
subtitle: Human and Agent Interaction
description: Welcome to Human and Agent Interaction (HAI) lab at Purdue University. Our research focuses on designing and deploying interactive AI agents that can improve people's lives by providing personalized support based on each user's needs, traits, and behaviors.
header: images/background_2.jpg
footer: images/Huggable.jpg

# site social media and other links
links:
  email: humanaipurdue@gmail.com
#   orcid: 0000-0001-8713-9213
  # google-scholar: obVs9ZAAAAAJ&hl=en&oi=sra
  # github: Human-Agent-Interaction
  twitter: HAIPurdue
#   youtube: hieumtran

### jekyll settings

# front matter defaults
defaults:
  # all markdown files
  - scope:
      path: ""
    values:
      layout: default
  # markdown files in /_members
  - scope:
      type: "members"
    values:
      layout: member
  # markdown files in /_posts
  - scope:
      type: "posts"
    values:
      layout: post

collections:
  # generate page for each member
  members:
    output: true
  # generate page for each post
  posts:
    output: true

# jekyll plugins
plugins:
  - jekyll-spaceship
  - jekyll-sitemap
  - jekyll-redirect-from
  - jekyll-feed
  - jekyll-last-modified-at

# code block syntax highlighting
highlighter: rouge

# jekyll theme
theme: null

# sass settings
sass:
  sass_dir: _styles

# force jekyll to include certain files/folders
include:
  - _styles
  - _scripts

# force jekyll to exclude certain files/folders
exclude:
  - README.md
  - LICENSE.md
  - CITATION.cff
